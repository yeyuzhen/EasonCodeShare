AUX_SOURCE_DIRECTORY(. LIBGREETING_SRC)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/libgreeting/include)
ADD_LIBRARY(greeting STATIC ${LIBGREETING_SRC})

SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/lib)